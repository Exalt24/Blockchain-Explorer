# Frontend Environment Variables for Docker Development

# API Configuration (using Docker service names)
VITE_API_URL=http://localhost:4000/api
VITE_WS_URL=http://localhost:4000

# Note: In Docker, frontend accesses backend through localhost:4000
# because ports are exposed on the host machine