# Backend Environment Variables for Docker Development

PORT=4000

# Database Configuration (Docker service names)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=blockchain_explorer
DB_USER=postgres
DB_PASSWORD=postgres

# Blockchain Configuration (Docker service name)
RPC_URL=http://hardhat:8545
CHAIN_ID=31337
START_BLOCK=0

# Contract Address (updated after deployment)
CONTRACT_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3

# CORS Configuration (allow all origins in development)
CORS_ORIGIN=*